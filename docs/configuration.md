# Configuration

Configuration d'OCAPI via Pydantic Settings (`.env`) et fichiers JSON sous `config/`.

## Sommaire

## Fichier `.env`

## Variables d'environnement principales

## Fichiers `config/`

- `config/llm_models.json`
- `config/llm_resilience.json`
- `config/llm_rate_limit.json`

## Logging

## Profils (dev / prod)

> TODO :
>
> - Tableau exhaustif des variables `.env` (clé, type, défaut, description)
> - Schéma de chargement de la config (Pydantic Settings)
> - Détail des champs des fichiers `config/*.json`
> - Renvoi vers [LLM](llm.md) pour la sélection du modèle
> - Exemples de configurations type
